{
  "name": "@discordic/repo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "turbo run build lint && changeset version && changeset publish"
  },
  "devDependencies": {
    "@discordic/tsconfig": "workspace:*",
    "@types/node": "^20.7.0",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "turbo": "latest",
    "typescript": "^5.2.2"
  },
  "packageManager": "pnpm@8.6.10"
}
